name: android_dev_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.0
  flutter_downloader: ^1.11.0
  path_provider: ^2.0.11
  torch_light: ^1.0.0
  qr_code_scanner: ^1.0.1
  speech_to_text: ^6.1.1
  flutter_tts: ^3.2.2
  http: ^1.2.1
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.1
  youtube_player_flutter: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
